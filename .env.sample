# [xrcloud-backend] Environment Variables

# -- Application Settings
NODE_ENV=development
DOCKER_IMAGE=xrcloud-backend
DOCKER_CONTAINER=xrcloud-backend

# -- Database (PostgreSQL)
TYPEORM_HOST=postgres
TYPEORM_PORT=5432
TYPEORM_DATABASE=xrcloud
TYPEORM_USERNAME=xrcloud
TYPEORM_PASSWORD=password
TYPEORM_SCHEMA=public
TYPEORM_POOL_SIZE=10

# -- Cache (Redis)
CACHE_HOST=redis
CACHE_PORT=6379

# -- Storage & Logs (Mounted via Docker)
# For local development, these can be relative paths.
# For production, these should be absolute paths on the host machine.
SRC_DIR=./src
LOG_DIRECTORY=./logs
STORAGE_DIR=./app/xrcloud-backend/storage
DB_VOLUME_DIR=./db

# -- Development Only
# WARNING: Setting this to 'true' will drop the database schema on startup.
# DO NOT use in production.
DEV_ALLOW_SCHEMA_RESET=false
